# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Ninbus Sans L, Awesome 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id urxvtc
bindsym $mod+Shift+Return exec --no-startup-id urxvtc -name urxvt_floating
bindsym $mod+Shift+p exec --no-startup-id urxvtc -name urxvt_python -e python

#bindsym $mod+Return exec --no-startup-id ~/Downloads/AUR/st-neonwave-git/src/st-neonwave/st

# kill focused window 
bindsym $mod+Shift+q kill
bindsym $mod+Shift+x kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run -nb '#000000' -nf '#ffffff' -sb '#ff00ff' -sf '#000000' -fn 'Inconsolata-11'

# metroid hack launcher
bindsym $mod+z exec --no-startup-id ~/bin/metroid.sh

# lock screen
bindsym $mod+Escape exec --no-startup-id ~/.config/i3/lockscreen

# screenshot (fullscreen)
bindsym $mod+Print exec --no-startup-id scrot ~/Pictures/screenshots/`date +%Y%m%d-%H%M%S`.png

# screenshot (selection)
bindsym --release $mod+Shift+Print exec --no-startup-id scrot -s ~/Pictures/screenshots/`date +%Y%m%d-%H%M%S`.png

# task manager
bindsym $mod+BackSpace exec --no-startup-id xfce4-taskmanager

# calendar
bindsym $mod+c exec --no-startup-id gsimplecal

# show scratchpad term
bindsym $mod+grave scratchpad show
#bindsym $mod+grave ~/.config/i3/scratchpad.sh 

# make something sticky and small in a corner
bindsym $mod+y fullscreen disable; floating enable; resize set 640 480; sticky enable; move window to position 1270 590
# undo that
bindsym $mod+Shift+y fullscreen disable; floating disable; sticky disable

# unhide a hidden window
bindsym $mod+Shift+u exec --no-startup-id ~/.config/i3/unhide.sh
# rename current workspace
bindsym $mod+n exec --no-startup-id ~/.config/i3/renamews.sh

# google search
bindsym $mod+g exec --no-startup-id ~/.config/i3/google_search.sh

# show rofi in window mode
bindsym $mod+Tab exec --no-startup-id rofi -hide-scrollbar -show window

#change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# toggle window decoration
bindsym $mod+Shift+b border toggle

# toggle sticky
bindsym $mod+t sticky toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+b focus child

# send to scratchpad 
bindsym $mod+Shift+grave move window to scratchpad

# keys to switch beween eDP1 and HDMI
bindsym $mod+l exec --no-startup-id xrandr --output HDMI --left-of --output eDP1 --auto
bindsym $mod+o exec --no-startup-id xrandr --output HDMI1 --auto --output eDP1 --off
bindsym $mod+p exec --no-startup-id xrandr --output HDMI1 --off --output eDP1 --auto
bindsym $mod+Shift+w exec --no-startup-id sh ~/.fehbg

# switch to vt 2
bindsym Mod1+XF86Forward exec --no-startup-id sudo chvt 2
set $workspace1 "1:term"
set $workspace2 "2:code"
set $workspace3 "3:www"
set $workspace4 "4:ssh"
set $workspace5 "5:chat"
set $workspace0 "10:game"

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace $workspace0

bindsym $mod+Control+1 workspace number 11
bindsym $mod+Control+2 workspace number 12
bindsym $mod+Control+3 workspace number 13
bindsym $mod+Control+4 workspace number 14
bindsym $mod+Control+5 workspace number 15
bindsym $mod+Control+6 workspace number 16
bindsym $mod+Control+7 workspace number 17
bindsym $mod+Control+8 workspace number 18
bindsym $mod+Control+9 workspace number 19
bindsym $mod+Control+0 workspace number 20

# blackwidow volume keys
bindsym XF86AudioMute exec --no-startup-id ~/.config/i3/volume.sh mute
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/.config/i3/volume.sh up 
bindsym XF86AudioLowerVolume exec --no-startup-id ~/.config/i3/volume.sh down

# brightness keys
bindsym XF86MonBrightnessUp exec --no-startup-id ~/.config/i3/backlight.sh up
bindsym XF86MonBrightnessDown exec --no-startup-id ~/.config/i3/backlight.sh down
bindsym XF86KbdBrightnessUp exec --no-startup-id ~/.config/i3/kb-backlight.sh up
bindsym XF86KbdBrightnessDown exec --no-startup-id ~/.config/i3/kb-backlight.sh down

# blackwidow macro keys
  # switch workspace
bindsym XF86Tools workspace $workspace1
bindsym XF86Launch5 workspace $workspace2
bindsym XF86Launch6 workspace $workspace3
bindsym XF86Launch7 workspace $workspace4
bindsym XF86Launch8 workspace $workspace5
  # move to workspace
bindsym Shift+XF86Tools move container to workspace $workspace1
bindsym Shift+XF86Launch5 move container to workspace $workspace2
bindsym Shift+XF86Launch6 move container to workspace $workspace3
bindsym Shift+XF86Launch7 move container to workspace $workspace4
bindsym Shift+XF86Launch8 move container to workspace $workspace5

# move to next/prev workspace on output
bindsym $mod+bracketleft workspace prev_on_output
bindsym $mod+bracketright workspace next_on_output

# move workspace to next/prev output - why is exec i3-msg needed? 
bindsym $mod+Shift+bracketleft exec i3-msg move workspace to output left 
bindsym $mod+Shift+bracketright exec i3-msg move workspace to output right 

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace $workspace0

bindsym $mod+Shift+Control+1 move container to workspace number 11
bindsym $mod+Shift+Control+2 move container to workspace number 12
bindsym $mod+Shift+Control+3 move container to workspace number 13
bindsym $mod+Shift+Control+4 move container to workspace number 14
bindsym $mod+Shift+Control+5 move container to workspace number 15
bindsym $mod+Shift+Control+6 move container to workspace number 16
bindsym $mod+Shift+Control+7 move container to workspace number 17
bindsym $mod+Shift+Control+8 move container to workspace number 18
bindsym $mod+Shift+Control+9 move container to workspace number 19
bindsym $mod+Shift+Control+0 move container to workspace number 20

# divvy emulation for floating windows
bindsym Control+Mod1+Right exec --no-startup-id ~/bin/divvy half-right
bindsym Control+Mod1+Left exec --no-startup-id ~/bin/divvy half-left

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r exec --no-startup-id "i3-nagbar -t warning -m 'You pressed the restart shortcut. Do you really want to restart i3?' -b 'Yes, restart i3' 'i3-msg restart'"

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec --no-startup-id "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

bindsym $mod+Shift+m mode "move" focus floating
mode "move" {
    bindsym $mod+Tab focus right

    bindsym Left  move left
    bindsym Down  move down
    bindsym Up    move up
    bindsym Right move right

    bindsym h     move left
    bindsym j     move down
    bindsym k     move up
    bindsym l     move right

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# power menu
set $sysmenu " [l]ock [p]oweroff [r]eboot [h]ibernate [s]leep "	
bindsym $mod+XF86PowerOff     mode $sysmenu
bindsym $mod+Scroll_Lock	  mode $sysmenu

mode $sysmenu {
	bindsym l		  exec ~/.config/i3/lockscreen; mode default
	bindsym p         exec sudo shutdown -P now
    bindsym r         exec sudo reboot
    bindsym s		  exec "sudo zzz"; mode default
    bindsym h		  exec "sudo ZZZ"; mode default
    bindsym Return    mode "default"
    bindsym Escape    mode "default"
}

set $randrmenu " xrandr: [jkl] [Enter|ESC] "
bindsym $mod+Shift+d			mode $randrmenu

mode $randrmenu {
	bindsym j		exec --no-startup-id echo "j" >> /tmp/randrmode
	bindsym k		exec --no-startup-id echo "k" >> /tmp/randrmode
	bindsym l		exec --no-startup-id echo "l" >> /tmp/randrmode

	bindsym Escape	mode default; exec --no-startup-id rm /tmp/randrmode
	bindsym Return	mode default; exec --no-startup-id ~/dotfiles/bin/xrandr.sh
}


bindsym $mod+period			gaps inner all plus 5
bindsym $mod+Shift+period	gaps inner all set 10
bindsym $mod+Shift+comma	gaps inner all set 0

client.focused          #444444 #000000 #ffffff #2e9ef4   #444444
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c
client.background       #ffffff

# make float
for_window [class="Xfce4-taskmanager"] floating enable
for_window [class="Galculator"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="Sxiv"] floating enable
for_window [class="feh"] floating enable

# android studio windows
for_window [class="jetbrains-studio" title="^Android Virtual Device Manager$"] floating enable, border normal
for_window [title="^Android Emulator - .*$"] floating enable, border normal

# hangouts: never float, never sticky, no border, always on workspace 5
for_window [title="^Google Hangouts.*$" window_role="app"] floating disable, sticky disable, border 1pixel, move to workspace $workspace5

# scratchpad terminal
for_window [instance="urxvt_scratchpad"] move scratchpad; scratchpad show; move position 15px 15px; resize set 1000px 500px; move scratchpad

# floating terminal
for_window [instance="urxvt_floating"] floating enable
for_window [instance="urxvt_python"] floating enable; move position 15px 15px

# steam stuff
for_window [class="^Steam$"] move to workspace $workspace0
for_window [class="^Steam$" title="^Friends$"] floating enable
for_window [class="^Steam$" title="Steam - News"] floating enable
for_window [class="^Steam$" title=".* - Chat"] floating enable
for_window [class="^Steam$" title="^Settings$"] floating enable
for_window [class="^Steam$" title=".* - event started"] floating enable
for_window [class="^Steam$" title=".* CD key"] floating enable
for_window [class="^Steam$" title="^Steam - Self Updater$"] floating enable
for_window [class="^Steam$" title="^Screenshot Uploader$"] floating enable
for_window [class="^Steam$" title="^Steam Guard - Computer Authorization Required$"] floating enable
for_window [title="^Steam Keyboard$"] floating enable

# 1 pixel decoration on new windows
new_window 1pixel

# gaps
gaps inner 10
smart_gaps on

# never any borders on edge of screen
hide_edge_borders both

#Startup programs
exec --no-startup-id ~/.config/i3/autostart.sh

